{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -c \"import os; port=os.environ.get('PORT','8000'); import uvicorn; uvicorn.run('main:app',host='0.0.0.0',port=int(port))\""
  }
}
